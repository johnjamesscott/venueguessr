{
  "name": "PendingSubmission",
  "type": "object",
  "properties": {
    "token": {
      "type": "string",
      "description": "Unique random token encoded in the QR code URL"
    },
    "competition_id": {
      "type": "string"
    },
    "total_score": {
      "type": "number"
    },
    "round_results": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "venue_name": {
            "type": "string"
          },
          "city": {
            "type": "string"
          },
          "score": {
            "type": "number"
          },
          "distance_km": {
            "type": "number"
          }
        }
      }
    },
    "avg_distance_km": {
      "type": "number"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "completed",
        "expired"
      ],
      "default": "pending"
    },
    "icp_boosted": {
      "type": "boolean",
      "default": false,
      "description": "True if the stored total_score was ICP-boosted on the kiosk"
    },
    "lead_id": {
      "type": "string",
      "description": "Lead created when this submission is finalized"
    },
    "leaderboard_entry_id": {
      "type": "string",
      "description": "Leaderboard entry created when this submission is finalized"
    },
    "email_sent": {
      "type": "boolean",
      "default": false,
      "description": "True after the post-game email has been sent"
    }
  },
  "required": [
    "token"
  ],
  "rls": {
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}
